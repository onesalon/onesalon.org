<h1>One Salon Global Stream</h1>
Check out photos from Salons around the world and keep in touch with the global community!
{% for event in events %}
    <h2><a href="{{ event.link }}">{{ event.name }}</a></h2>
    <b>{{ event.display_date }}</b><br>
    {% for photo in event.photos %}
        <img src="{{ photo.url }}"/>
    {% endfor %}
{% endfor %}
